{
  "name": "@bob/mock-api",
  "version": "0.1.0",
  "private": true,
  "description": "Local Fastify mock API for Build Buddy frontend-first sprint",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@bob/shared-schemas": "workspace:*",
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "tsx": "^4.7.1",
    "typescript": "^5.4.5"
  }
}
